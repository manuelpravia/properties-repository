service:
  name: auth-service
server:
  port: 8087
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka
  admin-paths:
    paths:
      - uri: "/customer"
        method: "GET"
